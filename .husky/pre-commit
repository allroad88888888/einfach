# Fast checks first: they fail in seconds, and `build` starts by deleting
# every package's @types, so a later failure would leave the tree without
# declaration output.
npm run lint:check
npm run typecheck
npm run build
npm test
